# ============================================================================
# Elastic Cloud Configuration
# ============================================================================
# Get these values from your Elastic Cloud deployment:
# 1. Log into https://cloud.elastic.co/
# 2. Select your deployment
# 3. Copy the Elasticsearch endpoint (without /_bulk suffix)
# 4. Go to "Manage" → "Integrations" → "APM" for APM settings
# 5. Create an API key with "monitor" and "ingest" privileges

# Elasticsearch Endpoint
# Format: https://your-deployment-id.es.region.cloud.elastic.co:443
# IMPORTANT: Do NOT include /_bulk - this is added automatically by the config
ELASTIC_ENDPOINT=https://your-deployment-id.es.us-east-1.aws.elastic-cloud.com:443

# APM Server Endpoint
# Format: https://your-deployment-id.apm.region.cloud.elastic.co:443
ELASTIC_APM_ENDPOINT=https://your-deployment-id.apm.us-east-1.aws.elastic-cloud.com:443

# APM Secret Token
# Found in: Deployments → [Your Deployment] → APM & Fleet → APM Server central configuration
ELASTIC_APM_SECRET_TOKEN=your_apm_secret_token_here

# Elasticsearch API Key
# Create with: Kibana → Stack Management → API Keys → Create API Key
# Required privileges: monitor, ingest (for metrics-* indices)
# Format: Base64 encoded string (id:api_key)
ELASTIC_API_KEY=your_base64_encoded_api_key_here

# ============================================================================
# Optional: Additional Configuration
# ============================================================================
# Uncomment and configure if needed

# OTEL_SERVICE_NAME=weather-api
# OTEL_RESOURCE_ATTRIBUTES=deployment.environment=development,service.version=1.0.0
# ELASTIC_OTEL_LOG_LEVEL=debug
