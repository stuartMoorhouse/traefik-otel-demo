# ============================================================================
# Elastic Cloud Configuration
# ============================================================================
# These values are automatically populated by Terraform deployment.
# If deploying manually or using an existing Elastic Cloud instance,
# fill in these values from your Elastic Cloud deployment.

# Elasticsearch Endpoint
# Get from: Elastic Cloud Console → Your Deployment → Copy Elasticsearch endpoint
# Format: https://your-deployment-id.es.region.cloud.elastic.co:443
# IMPORTANT: Do NOT include /_bulk or any path - just the base endpoint
ELASTIC_ENDPOINT=https://your-deployment-id.es.us-east-1.aws.elastic-cloud.com:443

# Elasticsearch Credentials (used during deployment)
# These are only needed if running deploy scripts manually
# Terraform deployment auto-populates these from ec_deployment resource
ELASTIC_USERNAME=elastic
ELASTIC_PASSWORD=your_elasticsearch_password_here

# Kibana Endpoint
# Get from: Elastic Cloud Console → Your Deployment → Copy Kibana endpoint
# Format: https://your-deployment-id.kb.region.cloud.elastic.co:443
KIBANA_ENDPOINT=https://your-deployment-id.kb.us-east-1.aws.elastic-cloud.com:443

# Elasticsearch API Key
# This is AUTO-GENERATED by the deployment script (deploy-apps.sh)
# If deploying manually, create an API key in Kibana:
#   1. Open Kibana → Stack Management → Security → API Keys
#   2. Create API Key with name: traefik-otel-demo
#   3. Required privileges:
#      - Cluster: monitor, manage_index_templates
#      - Indices (metrics-*, traces-*, logs-*): create_doc, auto_configure, create_index, write
#   4. Copy the Base64 encoded key
ELASTIC_API_KEY=your_base64_encoded_api_key_here

# ============================================================================
# Notes
# ============================================================================
# - When using Terraform deployment, all values are automatically set
# - When using existing Elastic Cloud, manually populate these values
# - APM Server is NOT used - EDOT Collector sends directly to Elasticsearch
# - API key is preferred over username/password for production use
